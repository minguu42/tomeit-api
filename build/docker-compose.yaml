version: "3"
services:
  api:
    build:
      context: ../.
      dockerfile: build/Dockerfile
    environment:
      - PORT
    container_name: tomeit-api
    stdin_open: true
    tty: true
    volumes:
      - ../.:/go/src/app
    ports:
      - ${PORT}:${PORT}
